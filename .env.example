# 环境标识：production（默认）或 test
# 设为 test 时加载 stores.test.yaml 配置，并在页面顶部显示测试环境横幅
APP_ENV=production
NEXT_PUBLIC_APP_ENV=production

# 飞书应用凭证
FEISHU_APP_ID=cli_a90ec606a4b85bd3
FEISHU_APP_SECRET=your_app_secret_here

# 访问控制盐（用于URL动态Token校验）
# 生成规则: Base64(SHA256("设备租赁" + 日期 + ACCESS_SALT).toString().substring(0,16))
ACCESS_SALT=cLQJrhSuajJ3ibIu7PtaAbKUXYDv2sbQF5aepegDIzA=
